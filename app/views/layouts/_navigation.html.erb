<%# navigation styled for Twitter Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Home', root_path, class: 'navbar-brand' %>
      <%= link_to 'Hàng mới về ', root_path, class: 'navbar-brand' %>
      <%= link_to 'Áo', root_path, class: 'navbar-brand' %>
      <%= link_to 'Quần Jean', root_path, class: 'navbar-brand' %>
      <%= link_to 'Váy', root_path, class: 'navbar-brand' %>
      <%= link_to 'Đồ nam', root_path, class: 'navbar-brand' %>
      <% if current_user %>
        <div class='login_name'><%=current_user.name%> Đã đăng nhập
          <%= link_to " (Đăng xuất)", signout_path %><div>
      <% else %>
        <div class='login_name'><%= link_to "Đăng nhập ngay", "/auth/facebook", id: "sign_in" %></div>
      <% end %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_links' %>
      </ul>
    </div>
  </div>
</nav>
