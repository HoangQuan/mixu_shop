<%# navigation styled for Twitter Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Home', root_path, class: 'navbar-brand' %>
      <%= link_to 'Sản phẩm', admin_products_path, class: 'navbar-brand' %>
      <%= link_to 'Loại Sản phẩm', admin_product_types_path, class: 'navbar-brand' %>
      <%= link_to 'Người dùng', admin_users_path, class: 'navbar-brand' %>
      <%= link_to 'Khác', root_path, class: 'navbar-brand' %>
      <% if current_user %>
      <div class='login_name'>
        <nav>
          <ul class="nav pull-right">
            <% if current_user %>
              <li id="fat-menu" class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  Account <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%=link_to (image_tag current_user.small_image), user_path(current_user) %></li>
                  <li><%= link_to "Profile", admin_user_path(current_user) %></li>
                  <li><%= link_to "Settings", edit_admin_user_path(current_user) %></li>
                  <li class="divider"></li>
                  <li>
                    <%= link_to "Đăng xuất", signout_path %>
                  </li>
                </ul>
              </li>
            <% else %>
              <li><%= link_to "Đăng nhập ngay", "/auth/facebook", id: "sign_in" %></li>
            <% end %>
          </ul>
        </nav>
      </div>
      <% else %>
        <div class='login_name'><%= link_to "Đăng nhập ngay", "/auth/facebook", id: "sign_in" %></div>
      <% end %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_links' %>
      </ul>
    </div>
  </div>
</nav>
